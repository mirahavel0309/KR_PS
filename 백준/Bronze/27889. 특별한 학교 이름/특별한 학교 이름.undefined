#include <bits/stdc++.h>
using namespace std;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    string s; cin >> s;
    cout << (s=="NLCS"?"North London Collegiate School":
             s=="BHA" ?"Branksome Hall Asia":
             s=="KIS" ?"Korea International School":
                       "St. Johnsbury Academy");
}
